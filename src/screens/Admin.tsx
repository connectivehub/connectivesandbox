// Placeholder screen — Phase 2 fills in the multi-tenant admin console
// (clients, workflow specs, usage) via the data adapters.

export default function Admin() {
  return (
    <main>
      <h1>Admin</h1>
      <p>Admin console is not implemented yet (Phase 2).</p>
    </main>
  )
}
