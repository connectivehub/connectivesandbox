// Placeholder screen — Phase 3 renders intake components and dashboard panels
// from the fixed registry for a validated WorkflowSpec, with judge results
// from the runner via the judge adapter.

export default function Workspace() {
  return (
    <main>
      <h1>Workspace</h1>
      <p>Workspace screen is not implemented yet (Phase 3).</p>
    </main>
  )
}
